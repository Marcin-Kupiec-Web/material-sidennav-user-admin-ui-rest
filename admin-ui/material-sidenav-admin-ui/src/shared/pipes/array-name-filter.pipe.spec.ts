import { ArrayNameFilterPipe } from './array-name-filter.pipe';

describe('ArrayNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
